test:
  override:
    - echo "test"
    # This allows faking out of circle

deployment:
  release:
    tag: /[0-9]+(\.[0-9]+)*/
    commands:
      - chmod +x gradlew
      - ./gradlew -Dbintray.user=$BINTRAY_USER -Dbintray.key=$BINTRAY_API_KEY